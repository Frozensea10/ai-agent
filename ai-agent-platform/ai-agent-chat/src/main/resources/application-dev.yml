feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 30000
      ai-agent-knowledge:
        connectTimeout: 5000
        readTimeout: 60000
    url:
      core-service: ${FEIGN_CORE_SERVICE_URL:http://localhost:8082}
      knowledge-service: ${FEIGN_KNOWLEDGE_SERVICE_URL:http://localhost:8084}

app:
  encryption:
    # 开发用固定 AES-256 密钥（base64 编码的 32 字节），生产环境务必通过环境变量 APP_ENCRYPTION_KEY 提供
    key: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=